import 'package:flutter/material.dart';

class VocabOverviewScreen extends StatelessWidget {
  const VocabOverviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}